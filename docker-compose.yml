services:
  qwe-qwe:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - qwe-data:/data
    environment:
      - QWE_LLM_URL=http://host.docker.internal:1234/v1
      - QWE_DATA_DIR=/data
    restart: unless-stopped

volumes:
  qwe-data:
